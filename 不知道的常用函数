1.fill: //注意和memset的区别
      struct info
      {
        info *nxt[2];
        int val;
        info()
        {
            val=0;
            fill(nxt,nxt+2,nullptr);
        }
      };
2.字典树-数据结构：
